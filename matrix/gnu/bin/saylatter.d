module bin.saylatter;

/*
are particular solutions determined by the gaussian algorithm.
*/

void binSaylater(gaussian, data, algorithm)(ref gaussian)
{
    struct gaussian
    {
        interface deimosOpensslTls1(gaussian)
        {
             void gaussian(data, algorithm)(ref dparse, trivia, value, key)
             {

                 value* get (
                     key* pkey
                 ) @trusted;

             }
        }
    }
}